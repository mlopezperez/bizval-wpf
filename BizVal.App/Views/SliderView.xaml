<UserControl x:Class="BizVal.App.Views.SliderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Slider HorizontalAlignment="Center" VerticalAlignment="Top" IsSelectionRangeEnabled="False" 
                Value="{Binding SelectedValue, Mode=TwoWay}" IsSnapToTickEnabled="True"  TickPlacement="BottomRight" Minimum="{Binding MinValue}" Maximum="{Binding MaxValue}" Width="200"/>
            <Label x:Name="SelectedValueText" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        </StackPanel>

        <ComboBox Grid.Column="1" ItemsSource="{Binding Scales}" SelectedIndex="{Binding SelectedScale}" DisplayMemberPath="Name"  Width="200" Height="25" VerticalAlignment="Top" HorizontalAlignment="Center">

        </ComboBox>


    </Grid>
</UserControl>