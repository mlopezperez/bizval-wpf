<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:d3="http://research.microsoft.com/DynamicDataDisplay/1.0" x:Class="BizVal.App.Views.CashflowView" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ContentControl x:Name="CashflowData" Grid.Column="0" Grid.Row="0" VerticalAlignment="Stretch" />
        <ContentControl x:Name="WaccData" Grid.Column="1" Grid.Row="0" VerticalAlignment="Stretch">
        </ContentControl>
        <Label x:Name="ErrorMessage" Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Foreground="DarkOrange" />
    </Grid>
</UserControl>
