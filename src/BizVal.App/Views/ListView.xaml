<UserControl x:Class="BizVal.App.Views.ListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <Label x:Name="InputName"></Label>
                <StackPanel Orientation="Horizontal">
                    <TextBox Margin="10,0,10,0" Width="150" HorizontalAlignment="Left" x:Name="NewItem"></TextBox>
                    <Button x:Name="Add" Width="40">Add</Button>
                </StackPanel>
                <ListBox Margin="10" x:Name="Items" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"  SelectedIndex="{Binding SelectedItemIndex, Mode=TwoWay}"  Width="200" Height="400"/>

            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <Button Content="Up" Height="25" Width="50" Margin="10" x:Name="Up"/>
                <Button Content="Down" Height="25" Width="50" Margin="10" x:Name="Down"/>
                <Button Content="Delete" Height="25" Width="50" Margin="10" x:Name="Delete"/>
            </StackPanel>
        </StackPanel>
</UserControl>
