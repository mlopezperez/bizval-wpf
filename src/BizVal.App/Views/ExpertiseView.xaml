<UserControl x:Class="BizVal.App.Views.ExpertiseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <!--Title="Add Expertise" Height="450" Width="600" WindowStyle="ToolWindow"-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ItemsControl Grid.Row="1" x:Name="Sliders">
        </ItemsControl>
        <Button x:Name="Add" Grid.Row="2" Width="100">Add</Button>

    </Grid>
</UserControl>
